name: Verify Human Identity

on:
  workflow_dispatch:
  push:
    branches: [ develop ]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Dependencies
        # Menambahkan @iden3/js-iden3-auth yang menyebabkan error MODULE_NOT_FOUND
        run: npm install @0xpolygonid/js-sdk @iden3/js-iden3-auth @clawhub/cli --save

      - name: Install Verified Agent Identity Skill
        run: npx skills add BillionsNetwork/verified-agent-identity --force

      - name: Create Ethereum Identity
        run: node scripts/createNewEthereumIdentity.js

      - name: Link Human to Agent
        run: node scripts/manualLinkHumanToAgent.js --challenge '{"name":"Reki120","description":"Reki120 AI Agent"}'