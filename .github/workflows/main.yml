name: Verify Human Identity
on: [workflow_dispatch]
jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest

      - name: Install Clawhub CLI
        run: npm install -g @clawhub/cli

      - name: Install Verified Agent Identity
        run: |
          echo "Menjalankan instalasi langsung dari repositori..."
          npx @clawhub/cli install BillionsNetwork/verified-agent-identity --force

      - name: Create Ethereum Identity
        run: node scripts/createNewEthereumIdentity.js

      - name: Link Human to Agent
        run: node scripts/manualLinkHumanToAgent.js --challenge '{"name":"Reki120","description":"Reki120 AI Agent"}'
